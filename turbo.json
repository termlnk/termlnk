{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test": {
      "inputs": [
        "src/**",
        "test/**",
        "tests/**",
        "vitest.config.*",
        "tsconfig.json",
        "package.json"
      ]
    },

    "coverage": {
      "cache": false
    },

    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "lib/**"],
      "inputs": [
        "src/**",
        "tsconfig.json",
        "tsconfig.*.json",
        "package.json"
      ]
    },

    "typecheck": {
      "inputs": [
        "src/**",
        "tsconfig.json",
        "tsconfig.*.json",
        "package.json"
      ]
    }
  }
}
