## apps/web runtime config
## Copy to .env (Vite loads it automatically). 不要把真实生产 URL 提交到仓库。

## 云后端地址。
## - 不带版本号（如 `http://localhost:4000`）—— apps/web 会自动追加 `/v1`
## - 带版本号（如 `http://localhost:4000/v1`）—— 原样使用
## termlnk-server 默认监听 4000；Hono 把 /v1/auth/* 与 /v1/sync/* 挂在版本前缀下。
VITE_TERMLNK_CLOUD_URL=http://localhost:4000
