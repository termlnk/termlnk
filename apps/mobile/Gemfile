source "https://rubygems.org"

# CocoaPods version locked to the range Expo SDK 55 prebuild expects.
# Bump in lock-step with React Native upgrades, not in isolation.
gem 'cocoapods', '>= 1.13', '< 1.16'
gem 'activesupport', '>= 6.1.7.5', '!= 7.1.0'
